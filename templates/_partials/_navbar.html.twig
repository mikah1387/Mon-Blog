<nav class="navbar" {% if is_granted('ROLE_ADMIN') %} style="background:#588157" {% endif %}>
  
    <div class="logo">
      <a href="{{path('main')}}"><img src="{{asset('images/image_style/logo2.png')}}" alt=" image_logo_the-blog"> </a>  
    </div>
    <ul>

        <li> <a href="{{path('main')}}">Accueil</a> </li>
        <li><a href="{{path('posts_index')}}">Articles</a> </li>
        <li> <a href="{{path('categories_index')}}">Catégories</a> </li>
        {# <li> <a href="">Auteurs</a> </li> #}
        <li> <a href="">Contact</a> </li>
        {% if app.user %}
        {% if is_granted('ROLE_ADMIN')  %}
        <li> <a href="{{path('admin_index')}}">Administration</a> </li>
            
        {% endif %}
        <li> <a href="{{path('profile_index')}}">Profile</a> </li>

        <li> <a href="{{path('app_logout')}}">Se deconnecter</a> </li>
            {% else %}
        <li> <a href="{{path('login')}}">Se connecter</a> </li>
        <li> <a href="{{path('register')}}">S'inscrire </a> </li>    
        {% endif %}
       
        <li> 
            {% set globalForm = render_global_form('App\\Form\\SearchPostType') %}
            {{form(globalForm )}}
        </li>
    </ul>

</nav>