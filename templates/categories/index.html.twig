{% extends 'base.html.twig' %}

{% block title %}Catégories{% endblock %}

{% block body %}
<section class="categories">

    {% for categoryP in allcatygories %}
        {% if categoryP.parent is null %}
            
        
            {# <div class="img">
             
            </div> #}
    
            <div class="cart">
                <div class="img">
                <img src="{{asset('images/uploads/categories/min/300x300-' ~ categoryP.image )}}" alt="image_categorie">
            </div>
             <h2> {{categoryP.slug}}</h2>
             <ul>

             {% for category in categoryP.categories %}
                    <li>
              <a href="{{path('categories_postsbycaty',{slug:category.slug})}}">{{category.slug}}</a>
                    
                    </li>
                
             {% endfor %}
            </ul>
            </div>
    
          
        {% endif %}
    
    {% endfor %}
</section>
{% endblock %}
