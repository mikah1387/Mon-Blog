{% extends 'base.html.twig' %}

{% block title %}Ajouter un article {% endblock %}

{% block body %}
<div class="addpost">

 
    {# {{form(addpostform)}} #}
    {{ form_start(addpostform) }}
    <h1>Ajouter un article</h1>

    {{ form_row(addpostform.title) }}
    {# {{ form_row(addpostform.content) }} #}
    {# {{ ckeditor_assets() }} #}
    <p>Contenu</p>
   
    <div id="editor">

   </div>
   
    {{ form_row(addpostform.categories) }}
    {{ form_row(addpostform.image) }}
    <button type="submit" class="button">{{button_label|default('Ajouter')}}</button>
    {{ form_end(addpostform) }}

   </div>

{% endblock %}

{% block javascripts %}
    <script src="{{asset('ckeditor/build/ckeditor.js')}}"></script>
{% endblock %}